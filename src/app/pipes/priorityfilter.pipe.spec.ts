import { PriorityfilterPipe } from './priorityfilter.pipe';

describe('PriorityfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
